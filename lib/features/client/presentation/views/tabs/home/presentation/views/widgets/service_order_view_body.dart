import 'package:flutter/widgets.dart';

class ServiceOrderViewBody extends StatelessWidget {
  const ServiceOrderViewBody({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}